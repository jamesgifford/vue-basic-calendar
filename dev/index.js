import Vue from 'vue'
import BasicCalendar from '../src/components/BasicCalendar.vue'
import BasicCalendarMonth from '../src/components/BasicCalendarMonth.vue'

new Vue({
    el: '#app',
    components: {
        BasicCalendar,
        BasicCalendarMonth
    }
});
